use std::collections::HashMap;

/*
struct CmdMgr {
    commands: HashMap<&str, CmdLazyLoader>
}

impl CmdMgr <'a> {
    fn parse_matches(&self, args: &ArgMatches) -> Result<(), String> {
        // TODO: Make method which calls trigger on every item of matches
    }
}

impl CmdMgr {
    fn new() -> SubCmdMgr {
        SubCmdMgr {
            commands: HashMap::new()
        }
    }

    fn register(&self, name: &str, loader: CmdLazyLoader) {
        if !commands.contains_key(name) {
            commands.insert(name, loader);
        }
    }

     fn trigger(&self, name: &str) {

     }
}
*/
